# Find-index-number-in-string
This simple Java program demonstrates how to find the index of a character in a string using the indexOf() method. The program uses a string variable and locates the first occurrence of the character 'o'.
